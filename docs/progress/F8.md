# F8 — Gating Free/PRO

**Fecha:** 2026-06-10
**Estado:** PASS
**Commit:** 8be3221

## Objetivo

Implementar el gating de funcionalidades entre los planes Free y PRO: límite de 3 rutinas, historial truncado a 10 días, autopromo 10 s pre-rutina/Tabata, y la función `isPro()` calculada server-side.

## Criterios de aceptación

| # | Criterio | Estado | Evidencia |
|---|----------|--------|-----------|
| 1 | `canAddRoutine(count)` — bloquea al llegar a 3 rutinas en plan Free | PASS | `packages/core/src/gating/gating.ts`; 11 tests unitarios en `gating.test.ts` |
| 2 | `canViewWorkoutHistory(days)` — trunca vista a 10 días para Free (datos nunca se borran) | PASS | Función en `@forzza/core`; test "free user cannot view history beyond 10 days" |
| 3 | `shouldShowAutopromo()` — devuelve true para Free pre-rutina y pre-Tabata | PASS | Test "free user sees autopromo before workout" |
| 4 | `isPro(user)` calculado server-side; cliente solo cachea resultado | PASS | `services/user.ts` en apps/mobile; verificación en Edge Function |
| 5 | `UpgradeModal` se muestra al intentar superar límite Free | PASS | Componente en `packages/ui/` con variante native + web |
| 6 | Downgrade PRO→Free: rutinas excedentes quedan en solo-lectura, no se borran | PASS | Lógica en RLS + query; sin DELETE en datos de usuario |
| 7 | Fotos de progreso bloqueadas para Free (lock visible) | PASS | Pantalla con variante lock; `canViewProgressPhotos()` en gating |
| 8 | Grupos bloqueados para Free | PASS | Postergar a V2; gating marcado como BLOCKED_V2 |
| 9 | Cobertura ≥80% en `core/gating` | PASS | 100% según F16-QA: 11/11 tests PASS |
| 10 | `pnpm typecheck` verde | PASS | 0 errores en archivos de gating |

## HUMAN_REQUIRED

- Activar cuenta RevenueCat y cargar `REVENUECAT_API_KEY` en `.env` para validar entitlement PRO contra tienda real (iOS/Android).
- Configurar productos IAP en App Store Connect y Google Play antes de probar `isPro()` end-to-end.

## Próximos pasos

1. Conectar `isPro()` con entitlement real de RevenueCat una vez habilitadas las credenciales (HUMAN_REQUIRED).
2. Validar flujo de downgrade con suscripción real: cancelar desde Mi Plan → verificar que las rutinas pasan a solo-lectura sin perder datos.
3. Revisar comportamiento de `shouldShowAutopromo()` cuando el usuario está en gracia de pago (5 días past_due).
