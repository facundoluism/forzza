# F17 — QA Completo

**Fecha:** 2026-06-10
**Estado:** PASS con HUMAN_REQUIRED pendientes
**Commit:** 8be3221

## Objetivo

Validar cobertura de tests unitarios (≥80% en `core/billing` y `core/gating`), ejecutar los 23 assertions pgTAP de RLS, definir los flows E2E Maestro, y auditar las 8 reglas de negocio innegociables del CLAUDE.md.

Ver informe detallado en `docs/progress/F16-QA.md`.

## Criterios de aceptación

| # | Criterio | Estado | Evidencia |
|---|----------|--------|-----------|
| 1 | `core/billing`: cobertura statements ≥80% | PASS | 100% — 8 tests unitarios (`billing.test.ts` + `settlement.test.ts`) |
| 2 | `core/gating`: cobertura statements ≥80% | PASS | 100% — 11 tests unitarios (`gating.test.ts`) |
| 3 | Cobertura global `@forzza/core`: statements ≥80% | PASS | 99.32% statements, 98.14% branches |
| 4 | 23 assertions pgTAP definidas en `supabase/tests/rls_test.sql` | PASS (definidas) | Archivo presente; ejecución requiere Supabase activo |
| 5 | Tests RLS ejecutados contra DB real (`pnpm test:rls`) | PENDIENTE | HUMAN_REQUIRED: Docker + `supabase start` |
| 6 | Acceso cruzado: alumno A no ve datos de alumno B | PASS (definido) | pgTAP #19, #20 — requiere ejecución real |
| 7 | Acceso anónimo denegado en `users`, `routines`, `messages` | PASS (definido) | pgTAP #16, #17, #18 — requiere ejecución real |
| 8 | `audit_log` append-only: rechaza UPDATE y DELETE | PASS (definido) | pgTAP #7, #8 — requiere ejecución real |
| 9 | 5 flows E2E Maestro definidos en `e2e/flows/` | PASS | `01-auth-signup.yaml` al `05-marketplace-browse.yaml` |
| 10 | Flows E2E ejecutados en emulador/dispositivo | PENDIENTE | HUMAN_REQUIRED: Maestro CLI + emulador activo |
| 11 | Las 8 reglas innegociables del CLAUDE.md auditadas y en PASS | PASS | F16-QA.md §"Reglas innegociables — Audit" |
| 12 | Sin secrets hardcodeados en el código | PASS | `.env.example` + mocks; `.gitignore` cubre `.env` |
| 13 | Headers de seguridad en Next.js (`X-Frame-Options`, `CSP`, etc.) | PASS | `next.config.js` con `securityHeaders` |
| 14 | URLs firmadas TTL 1h para fotos corporales y constancias | PASS | Supabase Storage signed URLs en Edge Functions |

## HUMAN_REQUIRED

1. **Docker Desktop + `supabase start`** — Necesario para ejecutar los 23 tests RLS con `pnpm test:rls`.
2. **`pnpm db:reset`** — Aplicar migraciones y seeds en DB local antes de ejecutar tests RLS.
3. **Maestro CLI** — Instalar (`brew install mobile-dev-inc/tap/maestro` o equivalente) y ejecutar flows contra emulador con app buildada.
4. **Emulador/dispositivo** — Android emulador o dispositivo físico para Maestro; iOS Simulator para flows de signup.

## Próximos pasos

1. Instalar Docker Desktop y ejecutar `supabase start` + `pnpm test:rls` (bloqueante para cierre definitivo de QA).
2. Ejecutar `maestro test e2e/maestro.config.yaml` contra emulador Android.
3. Si algún test RLS falla, abrir issue con el criterio y la evidencia; corregir antes de producción.
4. Agregar flow E2E `06-checkout-coach.yaml` para cubrir el flujo de mayor riesgo de negocio.
