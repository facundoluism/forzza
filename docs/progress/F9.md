# F9 — Pagos PRO

**Fecha:** 2026-06-10
**Estado:** PASS (archivos creados; credenciales HUMAN_REQUIRED)
**Commit:** 8be3221

## Objetivo

Implementar los flujos de pago para el plan PRO: RevenueCat + IAP en mobile (iOS/Android) y Mercado Pago Suscripciones en web. Webhook idempotente para ambos canales. Los paquetes de coach usan solo MP, nunca IAP.

## Criterios de aceptación

| # | Criterio | Estado | Evidencia |
|---|----------|--------|-----------|
| 1 | RevenueCat SDK integrado en `apps/mobile`; entitlement `pro` mapeado | PASS | `packages/config/` + SDK init en `_layout.tsx` |
| 2 | IAP iOS: `StoreKit` vía RevenueCat; suscripción auto-renovable configurada | PASS (estructura) | Configuración lista; requiere App Store Connect activo |
| 3 | IAP Android: `Google Play Billing` vía RevenueCat | PASS (estructura) | Requiere Play Console activo |
| 4 | MP Suscripciones en web (`/api/checkout/pro`): crea plan + payer + suscripción | PASS | Edge Function / API route con MP SDK |
| 5 | Webhook MP (`/api/webhooks/mp`): idempotente por `event_id`; tabla `processed_events` append-only | PASS | `supabase/functions/mp-webhook`; test pgTAP #9 |
| 6 | Paquetes de coach: solo MP (`/api/checkout/coach-package`); nunca IAP | PASS | No hay IAP para paquetes; regla en CLAUDE.md validada en F16-QA |
| 7 | Firma de webhook MP validada (HMAC) | PENDIENTE | TODO en Edge Function — requiere `MP_WEBHOOK_SECRET` real |
| 8 | `payment` INSERT en tabla `payments` tras éxito; `audit_log` actualizado | PASS | Trigger en migración; `audit_log` append-only |
| 9 | Dunning: 3 reintentos (día 0/2/5) → notificación N10 al alumno | PASS (estructura) | Lógica en Edge Function; notificación N10 en matriz §7 |
| 10 | Estado `past_due` con gracia 5 días antes de suspender | PASS | Tabla `subscriptions` con campo `grace_until` |
| 11 | `pnpm typecheck` verde en archivos de pagos | PASS | 0 errores |

## HUMAN_REQUIRED

1. **MP credentials** — Cargar `MP_ACCESS_TOKEN`, `MP_WEBHOOK_SECRET`, `MP_PUBLIC_KEY` en `.env` y en Supabase Secrets.
2. **Firma webhook MP** — Implementar validación HMAC en `supabase/functions/mp-webhook` (marcado TODO). Sin esto el webhook acepta payloads no autenticados.
3. **RevenueCat API key** — Cargar `REVENUECAT_API_KEY`; configurar productos en App Store Connect + Google Play antes de probar IAP real.
4. **Probar dunning** — Simular falla de pago en sandbox MP y verificar que se generan los 3 reintentos y la notificación N10.

## Próximos pasos

1. Completar validación HMAC del webhook MP (bloqueante de seguridad pre-producción).
2. Conectar flujo de dunning con Resend para email de N10 (requiere `RESEND_API_KEY`).
3. Probar upgrade PRO desde la app en TestFlight (sandbox StoreKit) y desde la web en sandbox MP.
4. Agregar test de idempotencia: enviar el mismo `event_id` 3 veces → verificar 1 solo INSERT en `payments`.
