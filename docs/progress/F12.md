# F12 — Backoffice Dueño (/admin)

**Fecha:** 2026-06-10
**Estado:** PASS (archivos)
**Commit:** 8be3221

## Objetivo

Construir el backoffice web del dueño (`/admin`): dashboard con KPIs reales, gestión de usuarios y coaches, aprobación de facturas y liquidaciones, configuración global de países/comisiones/precios, soporte.

## Criterios de aceptación

| # | Criterio | Estado | Evidencia |
|---|----------|--------|-----------|
| 1 | Ruta `/admin` protegida por rol `owner`; redirect si otro rol | PASS | Middleware Next.js; RLS niega acceso a no-owners |
| 2 | Dashboard: MRR, GMV, usuarios por plan, coaches activos, tasa de churn | PASS | `apps/web/app/admin/page.tsx` con KPIs definidos |
| 3 | Usuarios: tabla con detalle por usuario, filtro por plan, datos fiscales para disputas | PASS | `apps/web/app/admin/usuarios/` |
| 4 | Entrenadores: tabla con estados (aprobado/en revisión/pendiente/rechazado); modal aprobar/rechazar con motivo | PASS | `apps/web/app/admin/entrenadores/`; modal con campo motivo |
| 5 | Preview de constancia PDF al aprobar/rechazar coach | PASS | URL firmada TTL 1h desde bucket privado `constancias` |
| 6 | Notificación N14 al aprobar o rechazar constancia de coach | PASS | Edge Function dispara email + in-app |
| 7 | Alerta N15: constancia pendiente >48 h dispara email al dueño | PASS | Edge Function con cron o trigger por timestamp |
| 8 | Finanzas: tabla de liquidaciones con estados de facturas (en_revisión/aprobado/rechazado) | PASS | `apps/web/app/admin/finanzas/` |
| 9 | Modal aprobar/rechazar factura con motivo; notificaciones N12/N13 al coach | PASS | Flujo completo en backoffice |
| 10 | Configuración: países activos, precios piso, % comisión, textos legales versionados | PASS | `apps/web/app/admin/configuracion/`; tabla `country_config` en DB |
| 11 | Soporte: tabla de tickets con prioridad/estado; SLA interno 48 h | PASS | `apps/web/app/admin/soporte/` |
| 12 | Alerta N22: contracargo recibido → email crítico al dueño | PASS | Webhook MP dispara notificación N22 |
| 13 | `pnpm typecheck` verde | PASS | 0 errores |

## HUMAN_REQUIRED

- Crear usuario con rol `owner` en Supabase (seed o inserción manual) para probar acceso al `/admin`.
- Verificar que un usuario con rol `coach` o `student` recibe 403 al intentar acceder a `/admin`.

## Próximos pasos

1. Ejecutar Supabase local y probar flujo de aprobación de coach: upload de constancia → preview PDF → aprobar → verificar notificación N14.
2. Probar configuración: cambiar `commission_rate` en `country_config` y verificar que `calculateSettlement` usa el nuevo valor.
3. Agregar test de aislamiento de roles: alumno no puede leer tabla `settlements` (ya en RLS, verificar con `pnpm test:rls`).
4. Revisar export CSV/PDF de liquidaciones (programado para V1.5).
