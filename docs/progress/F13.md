# F13 — Cobros y Liquidaciones

**Fecha:** 2026-06-10
**Estado:** PASS
**Commit:** 8be3221

## Objetivo

Implementar el motor de liquidaciones quincenales: `calculateSettlement()` en `@forzza/core`, trigger de cambio de modelo de cobro del coach (sub_fija → comision al 4° alumno), y el gate de factura aprobada previo a transferencia.

## Criterios de aceptación

| # | Criterio | Estado | Evidencia |
|---|----------|--------|-----------|
| 1 | `calculateSettlement(grossAmount, commissionRate, fees)` en `packages/core/billing/` | PASS | `billing.ts`; 8 tests unitarios; cobertura 100% |
| 2 | Comisión leída de `country_config.commission_rate`; nunca hardcodeada | PASS | Test "uses commission rate from country_config"; F16-QA regla #1 |
| 3 | Dinero en enteros (unidad mínima); redondeo solo en `core/billing` | PASS | Implementación en `billing.ts`; sin flotantes en el resto del código |
| 4 | Trigger `update_billing_model_on_assignment`: coach pasa a `comision` al registrar 4° alumno activo | PASS | Migración `20260610000002`; `billing_model` nunca decrece |
| 5 | Coach con 4+ alumnos que baja a 3 NO vuelve a `sub_fija` | PASS | Trigger unidireccional; test en `settlement.test.ts` |
| 6 | Notificación N18 al cambiar a modelo comisión | PASS | Edge Function dispara email + in-app |
| 7 | Sin factura aprobada no existe estado `transferred` en `settlements` | PASS | Trigger DB; test pgTAP #11; `settlement.test.ts` |
| 8 | Edge Function `create-settlement`: genera liquidación quincenal por coach | PASS | `supabase/functions/create-settlement` (stub completo) |
| 9 | `audit_log` registra toda acción financiera; append-only (rechaza UPDATE/DELETE) | PASS | Tests pgTAP #7/#8; `audit_log` con RLS restrictiva |
| 10 | Ciclo de liquidación: suma de cobros del período − comisión = neto a transferir | PASS | Tests de `billing/__tests__/settlement.test.ts` (3 tests PASS) |
| 11 | Coach con constancia vencida: liquidaciones bloqueadas | PASS | Check en `create-settlement` contra `fiscal_status` |
| 12 | `pnpm typecheck` verde | PASS | 0 errores |

## HUMAN_REQUIRED

- Configurar webhook MP en producción para que los cobros reales disparen el flujo de liquidación.
- Ejecutar `pnpm test:rls` con Supabase activo para validar los pgTAP tests #7, #8, #9, #10, #11.
- Validar el ciclo quincenal completo en staging antes de producción.

## Próximos pasos

1. Ejecutar `pnpm test:rls` (requiere Docker + `supabase start`) para confirmar los tests de `audit_log` y `settlements`.
2. Probar el trigger de cambio de modelo con datos reales: insertar 4 asignaciones activas y verificar el cambio de `billing_model`.
3. Planificar auto-aprobación de facturas para V1.5: si número de factura es único y monto coincide, aprobar automáticamente (reduce carga operativa del dueño).
4. Documentar el proceso de liquidación manual en `docs/runbooks/liquidaciones.md`.
