# i18n completo ES/EN — App mobile (2026-06-15)

> Desvío de spec V1 aprobado por el owner (ver docs/open-questions.md). CLAUDE.md
> define V1 español-only; el owner pidió app bilingüe ES/EN con selector en el perfil
> y los 234 ejercicios en ambos idiomas. Proyecto multi-sesión.

## Estado de datos al inicio
- `exercise_library.name` = español ✅ · `name_en` = inglés ✅ (nombres ya bilingües)
- `exercise_library.description` = **solo inglés** ❌ (falta español)
- `primary_group`, `difficulty`, `primary_muscles`, `secondary_muscles`, `movement_pattern`, `equipment`, `tags` = **solo inglés** ❌
- App mobile: **sin infraestructura i18n**; perfil = placeholder sin selector.

## Plan por fases

### Fase 1 — Fundación + selector  ⏳ EN PROGRESO
- Librería i18n (`i18next` + `react-i18next` + `expo-localization`).
- Store de idioma (Zustand + persistencia AsyncStorage), default desde locale del device.
- Recursos `es`/`en` (JSON namespaced).
- **Selector de idioma en el perfil** (perfil reconstruido con tokens del design system).
- Migrar strings de las 5 tabs + chrome de los componentes de ejercicios a claves i18n.
- Nombre de ejercicio: mostrar `name` o `name_en` según idioma.

### Fase 2 — Resto de pantallas
- Migrar strings de: auth (login/signup/onboarding/forgot/reset), marketplace (index/[coachId]/checkout), session, routine (new/[id]), notifications, chat, upgrade, styleguide. Fan-out por grupos a subagentes.

### Fase 3 — Contenido de ejercicios bilingüe
- Migración DB: `description_es` + `description_en` (backfill `description_en` desde `description` actual; generar `description_es`).
- Mapas ES/EN para vocabulario controlado (group, difficulty, movement_pattern) y abierto (músculos, equipamiento) en un módulo de constantes.
- Wire de `ExerciseLibraryPicker` / `ExercisePreviewSheet` al idioma activo.
- Las 234 descripciones ES autogeneradas → **HUMAN_REVIEW** (terminología fitness, voseo).

### Fase 4 (a confirmar) — Web
- Replicar i18n en apps/web si el owner lo confirma.

## Criterio de cierre
Cada fase: typecheck + lint verdes; selector cambia idioma en caliente y persiste; sin strings hardcodeados en las pantallas migradas.

---

## Estado 2026-06-16 — Fases 1, 2 y 3 COMPLETAS (mobile)

- **Fase 1** ✅ infra + selector + tabs + chrome de ejercicios (commit `feat(mobile): i18n ES/EN fase 1`).
- **Fase 2** ✅ 13 pantallas restantes migradas (auth, marketplace, session, routine, notifications, chat, upgrade). Títulos de header reactivos vía `navigation.setOptions`. Paridad de claves es↔en verificada (294/294).
- **Fase 3** ✅ contenido de ejercicios bilingüe:
  - DB: migración `20260616000002_exercise_descriptions_i18n.sql` (columnas `description_es`/`description_en` + backfill). db-types actualizado.
  - Datos: `supabase/seed/exercises_descriptions_es.sql` — 234 descripciones ES + 124 EN. **HUMAN_REVIEW** (terminología fitness autogenerada). Renombrado para cargar DESPUÉS de `exercises.sql` (orden alfabético del runner de seeds).
  - App: `constants/exerciseI18n.ts` (mapas EN→ES de grupo/dificultad/movimiento/músculos/equipamiento) + wiring de `ExerciseLibraryPicker` y `ExercisePreviewSheet`. Descripción con fallback `description_es/en ?? description`.
- **Fase 4** (web) — ✅ COMPLETA (owner confirmó traducir toda la plataforma). Ver sección 2026-06-16 abajo.

### Pendientes / notas
- **HUMAN_REQUIRED deploy DB:** aplicar migración `20260616000002` + correr el seed `exercises_descriptions_es.sql` en la base cloud para que las descripciones ES aparezcan (en local entra solo con `pnpm db:reset`). Si el seed de ejercicios no estaba aplicado, también `exercises.sql`.
- **HUMAN_REVIEW:** validar las 234 traducciones de descripción (terminología de gimnasio) antes de producción.
- Los arrays `primary_muscles`/`equipment` del seed mezclan EN y ES (herencia de la fuente); `localizeMeta` lo resuelve con fallback al valor original.

---

## Estado 2026-06-16 (sesión 2) — Fase 4 WEB COMPLETA

Arquitectura: **next-intl**, `localePrefix: 'as-needed'` (ES en la raíz, EN con prefijo `/en`), `localeDetection: false` (ES por defecto, EN opt-in vía LanguageSwitcher + cookie). Decisión del owner: traducir TODA la plataforma (incluye backoffices).

- **4a Fundación** ✅ (commit `feat(i18n-web): fundación next-intl`): reestructura a `app/[locale]/` (api/ queda en raíz), `i18n/routing|request|navigation`, middleware intl+auth, catálogos `messages/`, LanguageSwitcher, landing piloto.
- **4b Públicas** ✅ (`Fase 4b`): auth, legales, marketplace, checkout, onboarding coach, upgrade, offline. +198 claves.
- **4c Backoffices** ✅ (`Fase 4c`): /admin y /coach completos + LanguageSwitcher en sidebars. +340 claves. **Total 612 claves con paridad es↔en**.
- **4d Pulido** ✅ (`pulido Fase 4d`): `<html lang>` dinámico, redirects locale-aware, sitemap `hreflang`, dedup de metadata, `next-pwa`→`@ducanh2912/next-pwa` (compat Next 15).

**QA:** typecheck/lint/build web verdes; e2e **80 passed / 1 skipped / 0 failed** (baseline pre-i18n restaurado). core 55 tests; mobile typecheck verde.

### Pendientes / HUMAN_REQUIRED
- **DB — ✅ VALIDADO local (2026-06-16):** migraciones `20260616000003` (fix RLS coach), `20260616000004` (routine_id link) y `20260616000005` (índice único completo en exercise_library.slug — arregla `ON CONFLICT` del seed) aplican limpio con `pnpm db:reset` contra Postgres real. Suite RLS reescrito: `pnpm test:rls` = **PASS 32/32** (aislamiento coach↔coach, alumno↔alumno, append-only, triggers). Sin bugs reales de seguridad. **Falta solo aplicar en cloud/staging** antes de deploy.
- **MEDIA-3 (seguridad, pre-existente):** falta enforcement server-side de `parental_consent_at` (§7) en `apps/web/app/api/mp-preapproval/route.ts` — hoy solo se valida client-side en el checkout.
- **i18n contenido (de fases mobile):** aplicar migración `20260616000002` + seed `exercises_descriptions_es.sql` en cloud; HUMAN_REVIEW de las 234 descripciones ES autogeneradas.
