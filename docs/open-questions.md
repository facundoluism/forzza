# Open Questions — Forzza

Ambigüedades registradas en lugar de asumidas en silencio.

| Fecha | Pregunta | Contexto | Bloqueante | Estado |
|---|---|---|---|---|
| 2026-06-10 | ¿Dónde está forzza-master-doc.md? | La fuente de verdad del producto no fue encontrada en el kit. Debe copiarse a docs/forzza-master-doc.md antes de continuar con Fase 2+ | SÍ (Fase 2+) | HUMAN_REQUIRED |
| 2026-06-10 | ¿Dónde está forza-complete.jsx? | El prototipo visual no fue encontrado en el kit. Debe copiarse a reference/forza-complete.jsx | NO (Fase 4+) | HUMAN_REQUIRED |
| 2026-06-10 | ¿Dónde está ejercicios-234.xlsx? | RESUELTO 2026-06-15: gym_exercise_database.xlsx (hoja Master Database) procesado. 234 ejercicios importados al remoto. Migración: 20260615000004_extend_exercise_library.sql. Seed: supabase/seed/exercises.sql. icon_id set: chest(33) back(38) legs(51) shoulders(40) arms(48) core(24). Tipos regenerados. | NO | resuelto |
| 2026-06-14 | coach_assignments.package_id es NOT NULL en migración pero coach-checkout crea payments antes de crear el assignment | El assignment se crea en mp-assignment-webhook tras aprobación del pago. Si el packageId viene null en metadata, el INSERT falla por NOT NULL constraint. Decisión tomada: pasar package_id en metadata del payment y leerlo en webhook. | NO | pendiente |
| 2026-06-14 | mp-create-preapproval usa upsert con onConflict="gateway_subscription_id" pero ese campo puede ser NULL | Si un user ya tiene una suscripción sin gateway_subscription_id, el upsert puede crear duplicado. Alternativa: onConflict="user_id" + unique index por user_id en subscriptions. | NO | pendiente |
| 2026-06-14 | generate-settlements: ¿qué currency usar si un coach no tiene pagos en el período? | La función omite coaches sin pagos (correcto). Para la currency del settlement, se lee de country_config basado en users.country del coach — correcto. | NO | resuelto |
| 2026-06-14 | ¿Qué tier asignar por defecto a nuevos paquetes de coach cuando el coach no especifica? | La API POST /api/coach/perfil inserta `tier: 'starter'` por defecto en el insert. ¿Es esto correcto según la spec de negocio o el coach debe elegir obligatoriamente? Los valores posibles son starter/pro/elite. | NO | pendiente |
| 2026-06-14 | ¿El checkout de coach en web debe mostrar los 3 tiers (starter/pro/elite) o el coach define sus propios paquetes con precio libre? | El schema actual tiene `coach_packages.tier` CHECK (starter/pro/elite) y `price INTEGER` libre. La UI de /coach/perfil permite crear paquetes con precio libre dentro del piso del país. ¿El alumno ve los paquetes como tiers fijos o como paquetes personalizados del coach? | NO | pendiente |
| 2026-06-14 | ¿Cómo maneja el sistema la moneda CL$ en Mercado Pago si Chile se activa en V2? | El schema tiene country_config con fila CL (CLP). MP Chile usa moneda CLP nativa. Las Edge Functions mp-create-preapproval y coach-checkout ya leen currency de country_config. Verificar que MP acepte preapproval_plan en CLP antes de activar Chile. | NO | pendiente |
| 2026-06-14 | ¿El signup de alumnos es solo en mobile o también en web? | Existe página /auth/signup en web (Next.js) pero el onboarding completo (fecha nacimiento, objetivos, parental consent) solo está en mobile. Un alumno que se registra en web queda con perfil incompleto. Definir si la PWA web es una experiencia completa o solo complementaria a la app nativa. | NO | pendiente |
| 2026-06-15 | i18n completo ES/EN — DESVÍO DE SPEC V1 aprobado por el owner | CLAUDE.md define V1 = Argentina, UI español rioplatense (voseo), sin i18n. El owner (facundoluism) pidió explícitamente app bilingüe ES/EN con selector de idioma en el perfil y los 234 ejercicios disponibles en ambos idiomas. Decisión tomada: implementar i18n completo de la app mobile por fases (ver docs/progress/i18n.md). Web también (Fase 4 completa, next-intl). Las traducciones de las 234 descripciones se autogeneran y quedan marcadas HUMAN_REVIEW (terminología fitness). | NO | resuelto |
| 2026-06-16 | EXTENSIÓN de la Regla §7 a PRO — aprobado por el owner | §7 (CLAUDE.md) solo exige bloquear a menores sin consentimiento parental en el checkout de COACH. El owner (facundoluism) pidió extenderlo: un menor de 18 sin `parental_consent_at` tampoco puede comprar PRO. Implementado server-side en `/api/mp-preapproval/route.ts` (403 minor_no_consent, early) y en la Edge Function `mp-create-preapproval` (defensa en profundidad). Lógica canónica en `@forzza/core` (isMinorWithoutConsent). | NO | resuelto |
| 2026-06-17 | DESVÍO APROBADO: live_sessions versión liviana — sin video propio | CLAUDE.md marca "sesiones en vivo" como NO-V1. Owner aprobó versión liviana con link externo (Zoom/Meet): tabla `live_sessions` creada en migración 20260617200003_live_sessions.sql con status scheduled/completed/canceled, room_url obligatorio (link externo), assignment_id opcional, campo reminded_at para idempotencia del cron N21. SIN video propio ni WebRTC. HUMAN_REQUIRED: cron N21 ("sesión en 30 min") NO implementado — requiere agendar un cron job (pg_cron o Supabase Scheduled Functions) que corra cada ~5 min, busque sesiones con scheduled_at entre now()+28m y now()+32m con reminded_at IS NULL, inserte notificaciones N21 para student_id y coach user_id, y setee reminded_at=now() para idempotencia. Ver comentario en la migración para spec completa. | NO | pendiente — HUMAN_REQUIRED cron N21 |
| 2026-06-18 | DESVÍO APROBADO: tabata_plans persiste datos — decisión del dueño | El master doc (Ficha 2.1.12 y §6.5) especifica que el Tabata NO persiste datos. Por decisión explícita del dueño (2026-06-18), ahora SÍ se persisten "planes de Tabata" en la tabla `tabata_plans` (RLS ownership: `student_id = auth.uid()`). Migración: `supabase/migrations/20260618000001_tabata_plans.sql`. No es una duda abierta; se registra para coherencia futura con las specs. | NO | resuelto — decisión del dueño |
| 2026-06-18 | Enforcement PRO server-side del modo avanzado de Tabata (severidad baja) | RESUELTO 2026-06-18: migración `20260618000002_tabata_advanced_pro_enforcement.sql`. Función `public.is_pro(uuid)` (SECURITY DEFINER) + policies INSERT y UPDATE de `tabata_plans` con `WITH CHECK (student_id = auth.uid() AND (mode='simple' OR is_pro(auth.uid())))`. Tests T48b, T49–T55 en `supabase/tests/rls_test.sql`. Resultado: 56/56 PASS. `db:reset` y `db lint` sin errores. Tipos regenerados, `web typecheck` PASS. | NO | resuelto |
| 2026-06-18 | HUMAN_REQUIRED — assets de audio del Tabata | El aviso sonoro (beeps de transición) requiere: (1) ejecutar `expo prebuild` para activar el plugin nativo de `expo-audio` en Expo 54 (el plugin ya está declarado en `app.json` pero el código nativo no se genera sin prebuild); (2) agregar los archivos `apps/mobile/assets/audio/tick.wav`, `start.wav` y `finish.wav` con los beeps correspondientes. Hoy la app funciona con háptica (vibración) + sistema de colores full-bleed; el audio queda desactivado por un guard `if (!audioReady) return` hasta completar ambos pasos. Pasos exactos: (a) en `apps/mobile/` correr `npx expo prebuild --platform android` y `--platform ios`; (b) copiar los tres archivos `.wav` al directorio de assets; (c) verificar que `expo-audio` resuelva los recursos con `require()` en el módulo `tabataAudio.ts`; (d) remover el guard y probar en dispositivo real. | SÍ (para audio; NO para el resto del Tabata) | pendiente — HUMAN_REQUIRED |
| 2026-06-18 | Hardening pre-prod: dev-bypass de `requireAdmin()` demasiado permisivo | Detectado en security-review del detalle de coach. `apps/web/lib/auth/admin.ts`: si `NEXT_PUBLIC_SUPABASE_URL` falta o contiene "placeholder", entra en modo mock SIN auth real (devuelve user ficticio `dev-admin-000` y client vacío). En staging/prod, una env ausente o mal copiada activaría el bypass silenciosamente y dejaría el backoffice sin protección. Recomendación: en prod/staging hacer `throw` si falta la var, y restringir el mock a `NODE_ENV === "development"` además del check de URL. | NO (bajo en deploys controlados; resolver ANTES de exponer el admin a internet) | pendiente — hardening |
| 2026-06-18 | Hardening pre-prod: falta rate limiting en `/api/admin/*` | Detectado en security-review del detalle de coach. El `middleware.ts` excluye todo `/api/` y la única protección es `requireAdmin()` dentro de cada handler. Sin rate limit, un owner autenticado puede disparar acciones de validación financiera/legal (approve/reject de coaches, settlements) en bucle. Hay un TODO de rate limiter pendiente en `middleware.ts` que solo contempla `/api/leads` y `/api/mp-preapproval`. Recomendación: implementar el rate limit y aplicarlo también a `/api/admin/*`. | NO (gateado a rol owner; resolver antes del go-live productivo) | pendiente — hardening |
| 2026-06-18 | DESVÍO APROBADO por el owner: acciones de admin en la ficha de usuario (cambiar rol / baja / restaurar) | El backlog §15 (BO-070) solo pide para la sección Usuarios del owner: filtro por plan, validación de constancia y audit log; §2.2 la describe como tabla con detalle + datos fiscales para disputas. Las acciones `set_role`, `deactivate` (baja) y `restore` NO están en el backlog, pero el owner (facundoluism) las pidió explícitamente al solicitar la "ficha de usuario con las funcionalidades mencionadas". Implementado en `apps/web/app/api/admin/users/[userId]/route.ts` con GUARDS para no violar reglas innegociables (verificado por product-spec-guardian): (1) no se puede auto-cambiar rol ni auto-darse de baja (adminUser.id===userId → 403); (2) no se puede modificar/dar de baja una cuenta `owner` (403); (3) DECISIÓN DEL DUEÑO 2026-06-18: se BLOQUEA la baja o democión de un coach con liquidaciones abiertas (pending/pending_invoice/invoiced/approved) → 409, hay que cerrar settlements (factura aprobada + transferido) antes — protege la regla 6; (4) la baja replica `delete-account` (cancela subs/assignments, soft-delete, deletion_queue +30d) y notifica a los coaches afectados (N_STUDENT_ACCOUNT_DELETED, §7.1); (5) restore solo si no se anonimizó. Todo auditado en audit_log. Nota: demover un coach deja su `coach_profiles` intacto (sin limpieza automática, por diseño). | NO | resuelto — desvío aprobado por el owner |
| 2026-06-22 | Precio PRO canónico: master-doc dice ARS 4.500 pero el seed cobra ARS 9.999 | El master-doc (§0 Executive Summary y §10 landing) especifica PRO a ARS 4.500/mes. El seed de country_config (supabase/seed/) carga ARS 9.999. | — | **RESUELTO 2026-06-22** — el dueño confirmó PRO = **ARS 9.999**. master-doc corregido (§0, §4.2, §10); el seed ya estaba en 9.999. |
| 2026-06-22 | Pisos de coach (min_coach_price): seed AR=$5.000 vs spec ~$29.900 | El seed de country_config carga min_coach_price=5000 ARS; el master-doc (§6.2, tabla de pisos por tier) referencia valores del orden de $29.900 para starter. Definir los pisos correctos por país y tier antes del go-live. Impacta: constraint DB en coach_packages + validación inline de UI. | NO | pendiente — decisión del dueño |
| 2026-06-22 | Ratings/reviews de coach: implementados pero marcados V2 en specs | Las tablas coach_ratings y coach_feedback existen en las migraciones y hay UI implementada, pero CLAUDE.md y el master-doc marcan ratings/reviews como NO-V1. | — | **RESUELTO 2026-06-22** — el dueño **aprueba el desvío** (ratings entran en V1). Pendiente de tanda de features: smoke test del feature + nuevo requerimiento "coach sube/elimina imagen de perfil". |
| 2026-06-22 | Cloud sync: aplicar las 18 migraciones pendientes con supabase db push | El cloud staging tenía 8 migraciones aplicadas (hasta 2026-06-15); el repo tiene 26. Faltaban 18 (todas aditivas, sin SQL destructivo). | — | **RESUELTO 2026-06-22** — el dueño autorizó y se ejecutó `supabase db push`: 18 migraciones aplicadas sin error, remoto 26/26 sincronizado, 10 tablas nuevas verificadas vía PostgREST. Pendiente aparte: subir seed de videos (`pnpm videos:push-cloud`) cuando la curación esté completa. |
| 2026-06-22 | Cron N21 (notificación "sesión en 30 min" para live_sessions) sigue HUMAN_REQUIRED | Ver entrada 2026-06-17. El cron que busca sesiones con scheduled_at entre now()+28m y now()+32m y envía notificación N21 a alumno y coach NO está implementado. live_sessions quedó funcional como tabla/API pero sin el recordatorio automático. Requiere: pg_cron o Supabase Scheduled Functions, configuración en el proyecto cloud, y test end-to-end. | SÍ (para la funcionalidad de recordatorio) | pendiente — HUMAN_REQUIRED |
